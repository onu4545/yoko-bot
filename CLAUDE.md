# 小沼陽子AIbot

不登校の子をもつ親のための、内省支援ツール。NPO法人優タウン代表・小沼陽子の
考え方をもとにした「対話ガイド型」の単一HTMLアプリ。

- 公開URL: https://onu4545.github.io/yoko-bot/
- リポジトリ: https://github.com/onu4545/yoko-bot （GitHub Pages / main / root）
- 本体: `index.html` 1ファイルのみ（外部依存なし・完全オフライン動作）

## このアプリの方針（重要）

- **AI/LLMは使わない。** 決まった台本（スクリプト）で応答するツール。
  不特定多数に無料配布するため、従量課金もAPIキー流出リスクもない構成にしている。
- **主人公は「子ども」ではなく「親」。** 目的は不登校の解決ではなく、
  親が自分のビリーフ（「ちゃんとしなければ」等）に気づき、子どもの側に立てる状態へ伴走すること。
- 元資料: `小沼陽子AIbot_システムプロンプト.docx`（v3 / MVP）。5段階のビリーフ変容モデルと
  相談9パターン、5ステップの会話フロー（受け止め→感情の言語化→ビリーフの仮説→別の見方→今日の小さな一歩）。

## 編集の仕方

応答文言・相談パターン・危機ワード・優タウンのリンクは、すべて `index.html` 冒頭の
`CONTENT` オブジェクト1か所で完結する。それ以外（描画ロジック）は通常触らない。

- `CONTENT.concerns[]` … 相談9パターン。各 `steps` が5ステップの応答文
- `CONTENT.generic.steps` … 自由記述で該当なしのときのフロー
- `CONTENT.crisis` … 危機ワード検知時の画面（電話4件＋優タウン導線）
- `CONTENT.utown` … 優タウンのリンク（https:// で始まる値は自動でリンク化）
- `CONTENT.model` … ビリーフ変容モデルの表
- `CONTENT.closing.feedbackContact` … 感想の送り先（office@yu-town.org）

## 画面構成

- 最初の画面: 説明 ＋「はじめる」ボタン。ヘッダー右上に「優タウンに話しにくる」「ビリーフ変容モデル」リンク
- 対話: 9パターンから選ぶ or 自由記述 → 陽子botの問いかけに1つずつ答えながら進む
- 危機ワード（「死にたい」「食べない」等）を書くと自動で相談窓口画面へ
- 終わりの画面: ねぎらい ＋「今日あなたが書いたこと」の振り返り ＋ 優タウン導線

## デプロイ

git 管理下で作業しているなら:

```bash
git add -A && git commit -m "変更内容" && git push
```

1〜2分で https://onu4545.github.io/yoko-bot/ に反映される。

## これまでの経緯・ユーザーの好み

- 冗長・説明的・凝った言い回しを嫌う。文体はやわらかく、でも簡潔に。
- 最初の画面(intro)の文言を凝った語り口に変えるのは却下された。素朴な現行版を維持。
- 削除済み: 回答欄のプレースホルダ、進捗ドット、ヘッダーの手動「相談」リンク、
  「同じ気持ちを抱えてきた人たちが待っています」等の営業的な一文。
- 危機画面の電話リストは、安全要件（システムプロンプトv3）のため完全削除はせず4件に絞ってある。

## 未完・TODO

- 相談窓口の電話番号・受付時間を配布前に最新確認
- 相談パターンを9→30へ増やす余地あり（元資料の「次のステップ」）
